source_db:
  type: mariadb
  host: 10.0.20.2
  port: 3306
  database: variant
  user: readonly_user
  password: "Qw@t3m@l@7%^&"

clickhouse:
  host: clickhouse
  port: 9000
  database: default

settings:
  backup_enabled: true
  backup_retention_days: 10
  batch_size: 50000

# Схемы и их расписания
schemas:
  finboon:
    company_id: "company_001"
    schedule: "0 2 * * *"
  variant:
    company_id: "company_002"
    schedule: "0 3 * * *"

# Таблицы описываются ОДИН раз — реплицируются на все схемы
tables:
  - table: client_contracts
    primary_key: id
    description: "Контракты"

  - table: clients
    primary_key: id
    description: "Клиенты"